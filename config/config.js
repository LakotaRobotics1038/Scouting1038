// config/config.js

    // configuration
    var config = {
      port: process.env.PORT || 3000,
      mongo: {
        url: 'mongodb://localhost/Scouting1038',
      }
    }

module.exports = config
